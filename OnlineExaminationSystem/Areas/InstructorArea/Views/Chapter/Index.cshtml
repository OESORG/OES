@model IEnumerable<OES.Model.Examination.Chapter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Registration.SemesterId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Registration.SemesterId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChapterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChapterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChapterId })
        </td>
    </tr>
}

</table>
